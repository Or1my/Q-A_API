# Q&A API Service

FastAPI-сервис для вопросов и ответов с PostgreSQL в Docker.

## 🚀 Функциональность

### Модели данных
- **Question** - вопросы
  - id, text, created_at
- **Answer** - ответы на вопросы  
  - id, question_id, user_id, text, created_at

### API Endpoints

#### Вопросы
- `GET /questions/` - список всех вопросов
- `POST /questions/` - создать новый вопрос
- `GET /questions/{id}` - получить вопрос с ответами
- `DELETE /questions/{id}` - удалить вопрос (с ответами)

#### Ответы
- `POST /questions/{id}/answers/` - добавить ответ к вопросу
- `GET /answers/{id}` - получить конкретный ответ
- `DELETE /answers/{id}` - удалить ответ

## 🛠 Технологии

- **FastAPI** - веб-фреймворк
- **PostgreSQL** - база данных
- **SQLAlchemy** - ORM
- **Docker** - контейнеризация
- **Pydantic** - валидация данных

## 📦 Установка и запуск

### Требования
- Docker
- Docker Compose

### Запуск проекта

1. Клонируй репозиторий:
```bash
git clone <url-репозитория>
cd Q-A_API
```

2. Запускаем Docker
```
docker-compose up --build
```
3. Переходим на сайт API
http://localhost:8000/docs
тут мы увидим все наши ручки, чтоб создать пост мы выбираем вкладку post/question/ нажимаем "try it out" и создаем вопрос, после мы получаем ответ с нашим вопросом и его id, дальше мы можем проверить создание ответов таким же путем, все существующие ответы можем посмотреть во вкладке get/question/, там мы можем увидеть его айди, с удаление вопросов и ответ все просто мы выбирам вкладку с надписью "delete" и вводим id вопроса/ответа, который мы хотим удалить.

4. Остановка проекта
```
docker-compose down
```
5. Очистка базы данных
```
docker volume rm python_postres_data
```
